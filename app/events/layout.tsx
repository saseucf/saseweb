export const metadata = {
    title: "Events",
    description: "Upcoming and past events hosted by SASE UCF — calendars, galleries, and highlights.",
} as const;

export default function EventsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}

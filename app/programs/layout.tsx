export const metadata = {
    title: "Programs",
    description: "Overview of SASE UCF programs — Intern Program, Mentor–Mentee, and member opportunities.",
} as const;

export default function ProgramsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
